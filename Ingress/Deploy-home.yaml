apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-IG
  labels:
    app: nginx-IG
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-IG
  template:
    metadata:
      name: home-template
      labels:
        app: nginx-IG
    spec:
      containers:
      - name: nginx-container
        image: nginx:latest
        ports:
        - name: nginx-port
          containerPort: 80
...
---
apiVersion: v1
kind: Service
metadat:
  name: home-svc
spec:
  type: ClusterIP
  selector:
    app: nginx-IG
  ports:
  - name: svc-port
    protocol: TCP
    port: 80
    targetPort: 80