apiVersion: v1
kind: Service
metadata:
  name: np-svc1
  labels:
    app: myapp
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
  - name: np-svc
    protocol: TCP
    port: 80
    targetPort: 80